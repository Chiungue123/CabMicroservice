spring:
  application:
    name: booking-service
  datasource:
    url: jdbc:mysql://localhost:3306/java_projects
    username: root
    password: root1

server:
  port: 8000  # Choose a unique port

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

#Logger
logging.level:
  debug: DEBUG
  '[com.javaprojects.springcrud]': DEBUG
  # Can be DEBUG for Development
  # Can be ERROR after Development
  # Can be OFF to turn the logger statemts off